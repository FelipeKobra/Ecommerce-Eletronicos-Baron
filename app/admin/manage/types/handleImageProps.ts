export interface handleImageProps {
    image: File;
    imagePath: string;
    variableId: string;
    removeBg: boolean;
  }
  
  export interface handleImageAPIData {
    imageURL: string;
    variableId: string;
  }